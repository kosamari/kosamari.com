'use strict';

var Backbone  = require('backbone');

var Router = module.exports = Backbone.Router.extend({
  routes: {
    ''              : 'index',
  },

  initialize: function(){
  },

  index: function(){
  }
});